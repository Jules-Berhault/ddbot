#include "ros/ros.h"
#include <WGS84toCartesian.hpp>

int main(int argc, char **argv) {

    // Setting the loop rate
    double h = 1.0/25.0;
    ros::Rate loop_rate(25);

    

    // Loop 
    while (ros::ok()) {

    }
    return 0;
}